{
  "name": "gp-connect-appointments-management-fhir",
  "version": "0.0.1",
  "description": "OAS (Swagger v3) API Definition for Template API",
  "scripts": {
    "lint": "node_modules/.bin/openapi-generator-cli validate -i specification/gp-connect-appointments-management-fhir.yaml",
    "publish": "./node_modules/.bin/openapi-generator-cli generate -i specification/gp-connect-appointments-management-fhir.yaml --generator-key v3.0  -g openapi -o build && rm openapitools.json && mv build/openapi.json build/gp-connect-appointments-management-fhir.json",
    "check-licenses": "node_modules/.bin/license-checker --failOn GPL --failOn LGPL"
  },
  "author": "NHS Digital",
  "license": "MIT",
  "homepage": "https://github.com/NHSDigital/gp-connect-appointments-management-fhir",
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.6.0",
    "license-checker": "^25.0.1"
  }
}
